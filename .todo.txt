TODO - maybe.

flag autoparsing
-	-v => --vb, -f => --ff
-	use UTS_VERBOSE_FLAG, UTS_FORCE_FLAG.
- 	think through passing flags down issue - e.g. s1 --vb calls s2 (--vb?)
-	$(cascadeVerbose()) if verbose(); return UTS_VERBOSE_FLAG etc. .. pass it onto next level of script.

ffn: etc.
- make error message a bit more useful if no arg given. Or 'chkcmdargs 1 4' or similar.

utils_pvar
- add test or do some more testing.

utils_opts
- getOptsSummary function (echo: "[-a] [-z] ..." etc) for script option docco.
- enable "-o|--option" format (ie. two forms of flags allowed).

